name = "bitterbot-funding-onramp"
main = "src/worker.ts"
compatibility_date = "2025-03-14"

# Live-only. Set secrets via: wrangler secret put STRIPE_SECRET_KEY_LIVE
# Never commit real keys. Use `wrangler secret put` for each:
#   STRIPE_SECRET_KEY_LIVE
#   STRIPE_PUBLISHABLE_KEY_LIVE

# For local dev with wrangler, put live keys in .dev.vars (gitignored).
